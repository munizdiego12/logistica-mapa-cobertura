# 🚚 Logística Urbana SP - Motor de Roteirização Last-Mile

Ferramenta de inteligência geoespacial e precificação de fretes com validação cadastral, roteamento sobre malha viária real (OSRM) e cálculo de custos operacionais detalhados em São Paulo.

---

## 🛠️ Instalação

```bash
git clone <URL_DO_REPOSITORIO>
cd logistica-sp
python -m venv .venv
source .venv/bin/activate  # No Windows: .venv\Scripts\activate
pip install -r requirements.txt